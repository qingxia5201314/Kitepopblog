# Content audit follow-up

These items require editorial review rather than automatic code changes:

- Rewrite summaries that repeat the article title instead of explaining the article value.
- Replace generic `image.png` alternative text with a description of what the image shows.
- Remove or clearly mark any public experiment password as an example value that is not used by a real service.
- Review `service-class/host:post`; `post` is likely intended to be `port`.
- The SRC category currently has no published article. The homepage CTA is hidden until at least one published SRC article exists.
- Replace the blocked third-party CET4 cover with a locally hosted image through the authenticated image host.
